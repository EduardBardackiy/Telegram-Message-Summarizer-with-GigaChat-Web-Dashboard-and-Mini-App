# Flask Web Dashboard для TGBot

Веб-интерфейс для мониторинга системы саммаризации Telegram-сообщений.

## 🎯 Возможности

### Страница 1: Дашборд (/)
- 📊 Всего сообщений в базе
- ✅ Количество проанализированных
- ⏳ Количество необработанных
- 🕐 Дата и время последней саммаризации
- 📈 Прогресс-бар обработки
- 🔄 Автообновление каждые 30 секунд

### Страница 2: Список сообщений (/messages)
- 📝 Таблица всех сообщений
- 🔍 ID, отправитель, текст, дата/время, статус
- 📄 Пагинация (50 сообщений на странице)
- 🎨 Красивый UI с Bootstrap 5

### API
- `/api/stats` - JSON с текущей статистикой

## 🚀 Установка и запуск

### 1. Установка зависимостей

Из корневой директории проекта или из `flask/`:

```powershell
# Активируйте виртуальное окружение (если не активировано)
.\.venv\Scripts\Activate.ps1

# Установите Flask
pip install -r flask/requirements.txt
```

### 2. Запуск приложения

```powershell
# Из корневой директории
python flask/app.py

# ИЛИ из директории flask/
cd flask
python app.py
```

### 3. Открыть в браузере

Приложение будет доступно по адресу:
- **Локально:** http://localhost:5000
- **В сети:** http://192.168.x.x:5000 (замените на ваш IP)

## 📁 Структура

```
flask/
├── app.py                 # Основное приложение Flask
├── templates/             # HTML шаблоны
│   ├── base.html         # Базовый шаблон
│   ├── index.html        # Дашборд (главная)
│   └── messages.html     # Список сообщений
├── static/               # Статические файлы (пока не используется)
├── requirements.txt      # Зависимости
└── README.md            # Эта документация
```

## 🎨 Дизайн

- **Bootstrap 5** - современный адаптивный дизайн
- **Bootstrap Icons** - красивые иконки
- **Responsive** - работает на всех устройствах
- **Dark/Light theme** - можно легко добавить

## 🔧 Настройка

### Изменить порт

Отредактируйте `app.py`, строка:

```python
app.run(debug=True, host="0.0.0.0", port=5000)  # Измените 5000 на нужный
```

### Изменить количество сообщений на странице

В `app.py`, функция `messages()`:

```python
per_page = 50  # Измените на нужное число
```

### Отключить автообновление дашборда

Удалите или закомментируйте в `templates/index.html`:

```javascript
// Автообновление каждые 30 секунд
setInterval(refreshStats, 30000);
```

## 🌐 Доступ из сети

По умолчанию Flask запускается с `host="0.0.0.0"`, что позволяет подключаться с других устройств в локальной сети.

1. Узнайте свой IP:
   ```powershell
   ipconfig
   ```

2. Откройте в браузере на другом устройстве:
   ```
   http://ваш_ip:5000
   ```

## 🔐 Безопасность

⚠️ **Важно!** Это приложение для локального использования:

- Не разворачивайте в продакшене без настройки безопасности
- Добавьте аутентификацию для публичного доступа
- Используйте HTTPS в продакшене
- Настройте CORS если нужен доступ с других доменов

## 🐛 Решение проблем

### База данных не найдена

Убедитесь, что `messages.db` находится в корневой директории проекта (`../messages.db` относительно `flask/`).

### Ошибка импорта Flask

Установите Flask:
```powershell
pip install Flask
```

### Порт 5000 занят

Измените порт в `app.py` или остановите процесс:
```powershell
Get-Process -Id (Get-NetTCPConnection -LocalPort 5000).OwningProcess | Stop-Process
```

## 📝 TODO (возможные улучшения)

- [ ] Поиск по сообщениям
- [ ] Фильтры (по статусу, дате, отправителю)
- [ ] Экспорт в CSV/Excel
- [ ] Графики и аналитика
- [ ] Просмотр текста саммаризации
- [ ] Темная тема
- [ ] Аутентификация пользователей
- [ ] WebSocket для real-time обновлений

## 🤝 Интеграция

Flask приложение работает независимо от остальных компонентов:
- **Telethon** - собирает сообщения в БД
- **Summary Bot** - создает саммаризации
- **Flask Dashboard** - отображает данные из БД

Все три могут работать одновременно!

---

**Разработано:** AI-ассистент Cursor (Claude Sonnet 4.5)  
**Дата:** 10.12.2024

