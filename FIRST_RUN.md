# 🚀 Первый запуск TGBot

Пошаговое руководство для быстрого старта проекта.

---

## Шаг 1: Клонирование и установка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/TGBot.git
cd TGBot

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте окружение
# Windows:
.\.venv\Scripts\Activate.ps1
# Linux/macOS:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

---

## Шаг 2: Получение API ключей

### 2.1 Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 Telethon API (для сбора сообщений)

1. Перейдите на [my.telegram.org/apps](https://my.telegram.org/apps)
2. Войдите с вашим номером телефона
3. Создайте новое приложение
4. Скопируйте `API ID` и `API Hash`

### 2.3 GigaChat API (Сбер)

1. Перейдите на [developers.sber.ru](https://developers.sber.ru/portal/products/gigachat)
2. Зарегистрируйтесь / войдите
3. Создайте проект
4. Получите `Client ID` и `Client Secret`

---

## Шаг 3: Конфигурация

Скопируйте пример конфигурации:

```bash
cp env.example .env
```

Откройте `.env` и заполните:

```env
# Telegram Bot
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
bot_username=YourBotUsername

# Telethon (для сбора сообщений)
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
SESSION_NAME=tg_session

# GigaChat API
CLIENT_ID=ваш_client_id
CLIENT_SECRET=ваш_client_secret
GIGACHAT_CA_BUNDLE=certs/giga_chain.pem
```

---

## Шаг 4: SSL-сертификаты GigaChat

### Windows (PowerShell):
```powershell
.\certs\fetch_gigachat_cert.ps1
```

### Linux/macOS:
```bash
mkdir -p certs
openssl s_client -showcerts -connect ngw.devices.sberbank.ru:9443 </dev/null 2>/dev/null | \
  openssl x509 -outform PEM > certs/giga_chain.pem
```

Проверьте, что файл создан:
```bash
ls certs/giga_chain.pem
```

---

## Шаг 5: Первый запуск компонентов

### 5.1 Telethon (сбор сообщений)

```bash
python telethon/main.py
```

**Важно:** При первом запуске Telethon попросит:
1. Ваш номер телефона (в международном формате: +7...)
2. Код подтверждения из Telegram
3. Пароль 2FA (если включен)

После успешной авторизации создастся файл `tg_session.session`.

### 5.2 Summary Bot (Telegram-бот)

Откройте новый терминал:

```bash
# Активируйте окружение
.\.venv\Scripts\Activate.ps1  # Windows
# или
source .venv/bin/activate      # Linux/macOS

# Запустите бота
python telebot/summary_bot.py
```

Теперь напишите боту в Telegram: `/start`

### 5.3 Flask Dashboard (веб-интерфейс)

Откройте ещё один терминал:

```bash
# Активируйте окружение
.\.venv\Scripts\Activate.ps1  # Windows
# или
source .venv/bin/activate      # Linux/macOS

# Запустите Flask
python flask/app.py
```

Откройте браузер: http://localhost:5000

### 5.4 Mini App (опционально)

Откройте ещё один терминал:

```bash
# Активируйте окружение
.\.venv\Scripts\Activate.ps1  # Windows
# или
source .venv/bin/activate      # Linux/macOS

# Запустите Mini App
python miniapp/app.py
```

Откроется на порте: http://localhost:5001

---

## Шаг 6: Тестирование

1. **Напишите боту в Telegram:**
   - Откройте бота (имя из `bot_username`)
   - Отправьте: `/start`
   - Нажмите кнопку: **📊 Статус**

2. **Отправьте тестовое сообщение:**
   - Напишите любое сообщение в чате (где запущен Telethon)
   - Вернитесь к боту
   - Нажмите: **📝 Саммаризация**
   - Бот должен вернуть краткую сводку

3. **Проверьте веб-интерфейс:**
   - Откройте: http://localhost:5000
   - Должны отображаться сообщения и статистика

---

## Шаг 7: Публикация в интернет (опционально)

Если хотите получить доступ к веб-интерфейсам из интернета:

### Установите CloudPub CLI

**Windows:**
```powershell
# Скачайте с https://cloudpub.ru/download
# Добавьте clo.exe в PATH или используйте полный путь
```

**Linux/macOS:**
```bash
# Следуйте инструкциям на https://cloudpub.ru/download
```

### Запустите туннели

```bash
# Авторизация
clo login

# Регистрация туннелей
clo register http 5000
clo register http 5001

# Запуск
clo run
```

После запуска CloudPub выдаст публичные URL:
- Flask Dashboard: `https://xxx.cloudpub.ru`
- Mini App: `https://yyy.cloudpub.ru`

**Автоматический запуск:**
```bash
python start_tunnels.py
```

---

## 🎉 Готово!

Теперь у вас работает:

✅ Telethon — собирает сообщения  
✅ Summary Bot — обрабатывает команды  
✅ GigaChat — саммаризирует тексты  
✅ Flask Dashboard — веб-интерфейс  
✅ Mini App — Telegram Mini App  
✅ CloudPub — публикация в интернет (опционально)

---

## 🆘 Возникли проблемы?

### Ошибка 409 (Telegram Bot)
```bash
# Удалите webhook
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
```

### Ошибка SSL (GigaChat)
Убедитесь, что файл `certs/giga_chain.pem` существует и правильный.

### База данных пустая
1. Убедитесь, что Telethon запущен
2. Отправьте сообщения в чат (где работает Telethon)
3. Проверьте логи Telethon

### Проверка статуса всех компонентов
```bash
python check_status.py
```

---

## 📚 Дополнительная документация

- [README.md](README.md) — основная информация
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) — полное руководство
- [CLOUDPUB_SETUP.md](CLOUDPUB_SETUP.md) — настройка CloudPub
- [PROJECT_HISTORY.md](PROJECT_HISTORY.md) — история и архитектура

---

**Приятного использования! 🚀**


