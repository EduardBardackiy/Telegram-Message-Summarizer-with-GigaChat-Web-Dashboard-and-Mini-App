# Настройка CloudPub.ru для проекта TGBot

## Что такое CloudPub.ru?

CloudPub.ru - это российский сервис для создания туннелей, позволяющий сделать локальные приложения доступными из интернета. Аналог ngrok.

## Установка и настройка

### Шаг 1: Регистрация

1. Перейдите на [https://cloudpub.ru](https://cloudpub.ru)
2. Зарегистрируйтесь и войдите в личный кабинет
3. Скопируйте ваш токен авторизации

### Шаг 2: Установка клиента

#### Для Windows (GUI):
1. Скачайте клиент CloudPub для Windows с сайта [https://cloudpub.ru](https://cloudpub.ru)
2. Установите приложение

#### Для командной строки:
1. Скачайте утилиту `clo` для вашей ОС
2. Добавьте её в PATH

### Шаг 3: Авторизация

Выполните в терминале:
```powershell
clo login
```

Токен сохраняется автоматически при выполнении `clo login`

## Запуск туннелей для проекта

У нас есть 3 приложения, которые нужно сделать доступными:

### 1. Flask Dashboard (порт 5000)
```powershell
clo publish http 5000
```

После выполнения вы получите URL типа: `https://xxxxx.cloudpub.site`

### 2. Telegram Mini App (порт 5001)
```powershell
clo publish http 5001
```

После выполнения вы получите URL типа: `https://yyyyy.cloudpub.site`

### 3. Запуск нескольких туннелей одновременно

Чтобы запустить оба туннеля одновременно:

```powershell
# Вариант 1: Регистрация и запуск отдельно
clo register http 5000
clo register http 5001
clo run

# Вариант 2: Регистрация и запуск одной командой (для каждого порта)
clo publish http 5000
clo publish http 5001
```

## Использование полученных URL

### Для Flask Dashboard
Просто откройте полученный URL в браузере.

### Для Telegram Mini App

1. Скопируйте полученный URL (например, `https://yyyyy.cloudpub.site`)
2. Откройте [@BotFather](https://t.me/BotFather) в Telegram
3. Отправьте команду `/mybots`
4. Выберите вашего бота `@ED_Zerocoder_intensive_bot`
5. Выберите **Bot Settings** → **Menu Button**
6. Выберите **Edit Menu Button URL**
7. Вставьте ваш CloudPub URL

Теперь при нажатии на кнопку меню в боте будет открываться ваше Mini App!

## Важные замечания

⚠️ **HTTPS**: CloudPub автоматически предоставляет HTTPS, что необходимо для Telegram Mini Apps.

⚠️ **Стабильность URL**: При бесплатном использовании URL может меняться при каждом перезапуске туннеля. Для постоянного URL рассмотрите платный план или настройте собственный домен.

⚠️ **Ограничения**: Проверьте лимиты бесплатного плана на сайте CloudPub.ru.

## Альтернативный способ: Запуск через Python SDK

CloudPub предоставляет Python SDK для программного управления туннелями.

### Установка SDK

```bash
pip install cloudpub-python-sdk
```

### Пример использования

```python
from cloudpub_python_sdk import Connection, Protocol, Auth

# Создание соединения с учетными данными
conn = Connection(
    email="user@example.com",
    password="password"
)

# Публикация Flask Dashboard (порт 5000)
flask_endpoint = conn.publish(
    Protocol.HTTP,
    "localhost:5000",
    name="Flask Dashboard",
    auth=Auth.NONE
)
print(f"Flask доступен: {flask_endpoint.url}")

# Публикация Mini App (порт 5001)
miniapp_endpoint = conn.publish(
    Protocol.HTTP,
    "localhost:5001",
    name="Telegram Mini App",
    auth=Auth.NONE
)
print(f"Mini App доступен: {miniapp_endpoint.url}")

# Поддержание туннелей в работе
try:
    input("Нажмите Enter для остановки...")
finally:
    # Очистка
    conn.unpublish(flask_endpoint.guid)
    conn.unpublish(miniapp_endpoint.guid)
```

📖 **Документация Python SDK:** https://cloudpub.ru/docs/python-sdk/

## Полезные ссылки

- 📖 Документация: [https://cloudpub.ru/docs](https://cloudpub.ru/docs)
- 💬 Поддержка: [https://cloudpub.ru/support](https://cloudpub.ru/support)
- 🏠 Главная: [https://cloudpub.ru](https://cloudpub.ru)

## Команды для быстрого старта

```powershell
# 1. Авторизация
clo login

# 2. Вариант А: Быстрый запуск (каждая команда в отдельном терминале)
clo publish http 5000    # Терминал 1: Flask Dashboard
clo publish http 5001    # Терминал 2: Mini App

# 3. Вариант Б: Регистрация и запуск всех туннелей вместе
clo register http 5000
clo register http 5001
clo run                  # Запустит оба туннеля в одном терминале
```

## Проверка работы

После запуска туннелей:
1. Откройте полученный URL для Flask в браузере
2. Настройте Mini App URL в @BotFather
3. Откройте вашего бота в Telegram и нажмите на кнопку меню

Готово! 🎉

