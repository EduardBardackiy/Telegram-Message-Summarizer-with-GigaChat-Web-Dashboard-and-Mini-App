# CloudPub: CLI vs Python SDK

Существует **два способа** работы с CloudPub. Этот документ объясняет разницу и помогает выбрать подходящий вариант.

---

## 🔧 CloudPub CLI

### Что это?
Утилита командной строки `clo` для управления туннелями через терминал.

### Установка
Скачайте с https://cloudpub.ru и установите приложение.

### Использование

```powershell
# Авторизация
clo login

# Регистрация туннелей
clo register http 5000
clo register http 5001

# Запуск
clo run
```

### Преимущества
- ✅ Не требует написания кода
- ✅ Простые команды
- ✅ Подходит для разовых задач
- ✅ Графический интерфейс для Windows

### Недостатки
- ❌ Нужно запускать вручную
- ❌ Сложнее автоматизировать
- ❌ Отдельный процесс

### Когда использовать?
- Разработка и тестирование
- Ручной запуск туннелей
- Не нужна интеграция с кодом

---

## 🐍 CloudPub Python SDK

### Что это?
Python библиотека `cloudpub-python-sdk` для программного управления туннелями.

### Установка

```bash
pip install cloudpub-python-sdk
```

### Использование

```python
from cloudpub_python_sdk import Connection, Protocol, Auth

# Создание соединения
conn = Connection(
    email="user@example.com",
    password="password"
)

# Публикация туннеля
endpoint = conn.publish(
    Protocol.HTTP,
    "localhost:5000",
    name="My App",
    auth=Auth.NONE
)

print(f"URL: {endpoint.url}")
```

### Преимущества
- ✅ Полная интеграция с Python кодом
- ✅ Легко автоматизировать
- ✅ Программное управление
- ✅ Можно встроить в приложение

### Недостатки
- ❌ Требует написания кода
- ❌ Дополнительная зависимость

### Когда использовать?
- Интеграция в Python приложение
- Автоматический запуск при старте приложения
- Программное управление туннелями
- Production deployment

---

## 📊 Сравнительная таблица

| Параметр | CLI (`clo`) | Python SDK |
|----------|-------------|------------|
| **Установка** | Отдельное приложение | `pip install` |
| **Язык** | Любой (команды) | Python |
| **Автоматизация** | Сложнее | Легко |
| **Интеграция** | Внешний процесс | Встроено в код |
| **Авторизация** | `clo login` | `Connection(email, password)` |
| **Запуск туннеля** | `clo publish http 5000` | `conn.publish(Protocol.HTTP, "localhost:5000")` |
| **Управление** | Команды в терминале | API методы |
| **Логи** | В консоль | В консоль + программно |
| **Для TGBot** | ✅ Подходит | ✅✅ Лучше подходит |

---

## 🎯 Рекомендации для проекта TGBot

### Вариант 1: CLI (простой способ)

**Для кого:** Начинающие, быстрое тестирование

**Как использовать:**

```powershell
# Установите CloudPub CLI
# Затем запустите:
.\start_tunnels.ps1
```

Или вручную:
```powershell
clo login
clo register http 5000
clo register http 5001
clo run
```

📖 **Файлы:**
- `start_tunnels.ps1` — PowerShell скрипт
- `start_tunnels.py` — Python скрипт (использует CLI через subprocess)

### Вариант 2: Python SDK (профессиональный способ)

**Для кого:** Опытные разработчики, production

**Как использовать:**

```bash
# Установите SDK
pip install cloudpub-python-sdk
```

```powershell
# Добавьте в .env:
# CLOUDPUB_EMAIL=your@email.com
# CLOUDPUB_PASSWORD=your_password

# Запустите:
python start_tunnels_sdk.py
```

📖 **Файлы:**
- `start_tunnels_sdk.py` — Python скрипт с использованием SDK

---

## 🔄 Миграция с CLI на SDK

### Было (CLI)

```powershell
clo login
clo register http 5000
clo run
```

### Стало (SDK)

```python
from cloudpub_python_sdk import Connection, Protocol

conn = Connection(email="user@example.com", password="password")
endpoint = conn.publish(Protocol.HTTP, "localhost:5000")
print(f"URL: {endpoint.url}")

# Ожидание...
input("Press Enter to stop...")
conn.unpublish(endpoint.guid)
```

---

## 📋 Частые вопросы

### Q: Можно ли использовать оба способа одновременно?
**A:** Да, но не рекомендуется для одних и тех же портов. Это может вызвать конфликты.

### Q: Какой способ быстрее?
**A:** Оба работают одинаково быстро. CLI проще в использовании, SDK — удобнее для автоматизации.

### Q: Нужно ли устанавливать CLI, если используешь SDK?
**A:** Нет, это независимые инструменты.

### Q: Где хранятся учетные данные?
**CLI:** В конфиге CloudPub (команда `clo login`)  
**SDK:** Передаются в коде или через переменные окружения

### Q: Как посмотреть активные туннели?
**CLI:** `clo ls`  
**SDK:** `conn.ls()`

### Q: Как остановить туннели?
**CLI:** `Ctrl+C` или `clo stop`  
**SDK:** `conn.unpublish(endpoint.guid)` или `conn.clean()`

---

## 🔗 Ссылки

### CLI
- 📖 Команды CLI: [CLOUDPUB_COMMANDS.txt](CLOUDPUB_COMMANDS.txt)
- 📝 Быстрый старт: [QUICKSTART_CLOUDPUB.txt](QUICKSTART_CLOUDPUB.txt)

### Python SDK
- 📖 Официальная документация: https://cloudpub.ru/docs/python-sdk/
- 📝 Скрипт для проекта: [start_tunnels_sdk.py](start_tunnels_sdk.py)

### Общее
- 🌐 Сайт CloudPub: https://cloudpub.ru
- 📖 Полная настройка: [CLOUDPUB_SETUP.md](CLOUDPUB_SETUP.md)
- 🚀 Руководство по развертыванию: [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)

---

## ✅ Выводы

**Используйте CLI если:**
- Вы новичок в CloudPub
- Нужно быстро протестировать
- Не хотите писать код
- Разовая задача

**Используйте Python SDK если:**
- Хотите интегрировать в приложение
- Нужна автоматизация
- Production окружение
- Программное управление

**Для проекта TGBot:** Оба варианта работают отлично! Выбирайте тот, который вам удобнее.

---

**Последнее обновление:** 10.12.2024  
**Версия:** 1.0.0

