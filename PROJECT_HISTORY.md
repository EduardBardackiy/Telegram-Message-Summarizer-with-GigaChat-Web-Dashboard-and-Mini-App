# История разработки проекта TGBot

**Дата начала:** декабрь 2024  
**Текущий статус:** ✅ Работает полностью  
**Расположение:** USB-диск `F:\Zerocoder\Intensive\TGBot`

---

## 📋 Описание проекта

Система для **саммаризации сообщений из Telegram** с использованием GigaChat API.

**Основные компоненты:**
1. **Telethon-клиент** — слушает сообщения в Telegram, сохраняет в БД
2. **Summary Bot** — Telegram-бот на `pyTelegramBotAPI`, создаёт саммаризацию по команде
3. **GigaChat API** — нейросеть для саммаризации текста
4. **SQLite БД** — хранилище сообщений
5. **Flask Dashboard** — веб-интерфейс мониторинга (порт 5000)
6. **Telegram Mini App** — мини-приложение в Telegram (порт 5001)
7. **CloudPub туннели** — публикация приложений в интернет

---

## 🏗️ Структура проекта

```
TGBot/
├── .venv/                    # Виртуальное окружение Python
├── .env                      # Конфиденциальные данные (НЕ в Git!)
├── env.example               # Шаблон переменных окружения
├── requirements.txt          # Зависимости Python
├── messages.db               # SQLite база сообщений
│
├── ai/                       # Модуль работы с GigaChat
│   ├── main.py              # CLI для саммаризации
│   ├── gigachat.py          # API GigaChat (OAuth, саммаризация)
│   └── README.md            # Документация модуля
│
├── telethon/                # Модуль Telethon (сбор сообщений)
│   ├── main.py              # Клиент для прослушивания Telegram
│   ├── config.py            # Конфигурация (API ID, Hash)
│   ├── db.py                # Работа с БД
│   └── README.md            # Документация модуля
│
├── telebot/                 # Telegram-бот
│   ├── summary_bot.py       # Основной бот (саммаризация по команде)
│   └── README.md            # Инструкции
│
├── flask/                   # Flask веб-интерфейс (порт 5000)
│   ├── app.py               # Flask приложение
│   ├── templates/           # HTML шаблоны
│   ├── static/              # CSS, JS файлы
│   ├── requirements.txt     # Зависимости Flask
│   └── README.md            # Документация
│
├── miniapp/                 # Telegram Mini App (порт 5001)
│   ├── app.py               # Mini App приложение
│   ├── templates/           # HTML шаблоны
│   ├── static/              # CSS, JS файлы
│   ├── requirements.txt     # Зависимости
│   └── README.md            # Документация
│
├── certs/                   # SSL-сертификаты для GigaChat
│   ├── giga_chain.pem       # Цепочка сертификатов
│   └── fetch_gigachat_cert.ps1  # Скрипт загрузки сертификата
│
├── start_tunnels.ps1        # PowerShell скрипт запуска CloudPub туннелей
├── start_tunnels.py         # Python скрипт запуска CloudPub туннелей
├── check_status.py          # Скрипт проверки статуса всех компонентов
├── CLOUDPUB_SETUP.md        # Подробная инструкция CloudPub
├── QUICKSTART_CLOUDPUB.txt  # Краткая памятка CloudPub
│
└── scripts/
    └── update_token.py      # Утилита обновления GigaChat токена
```

---

## 🔧 Как запустить (на любом ПК)

### 1. Активация виртуального окружения

```powershell
cd F:\Zerocoder\Intensive\TGBot
.\.venv\Scripts\Activate.ps1
```

### 2. Запуск Telethon (сбор сообщений из Telegram)

```powershell
python telethon\main.py
```

**Что делает:** слушает все сообщения в Telegram, сохраняет в `messages.db`

### 3. Запуск Summary Bot (бот саммаризации)

```powershell
python telebot\summary_bot.py
```

**Команды бота:**
- `/start` или `/help` — справка
- `/status` — количество необработанных сообщений
- `/summary` — создать саммаризацию всех новых сообщений

---

## 🔑 Переменные окружения (.env)

```env
# GigaChat API
GIGACHAT_CLIENT_ID=ваш_client_id
GIGACHAT_CLIENT_SECRET=ваш_secret
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_CA_BUNDLE=certs/giga_chain.pem

# Telegram Bot
BOT_TOKEN=ваш_bot_token

# Telethon (User Client)
API_ID=ваш_api_id
API_HASH=ваш_api_hash
SESSION_NAME=tg_session
bot_username=@ED_Zerocoder_intensive
```

**⚠️ Важно:** файл `.env` уже настроен, переменные корректны!

---

## 📊 База данных (messages.db)

**Таблица:** `messages`

| Колонка      | Тип     | Описание                          |
|--------------|---------|-----------------------------------|
| `id`         | INTEGER | PRIMARY KEY                       |
| `chat_id`    | INTEGER | ID чата Telegram                  |
| `sender`     | TEXT    | Имя отправителя                   |
| `text`       | TEXT    | Текст сообщения                   |
| `date`       | TEXT    | Дата ISO 8601                     |
| `summarized` | INTEGER | 0 = новое, 1 = обработано         |

**Текущее состояние:** 38 сообщений, 4 необработанных

---

## 🐛 Решённые проблемы

### Проблема 1: SSL Certificate Verification Error

**Ошибка:**
```
SSLError: certificate verify failed: self-signed certificate in certificate chain
```

**Решение:**
- Загружен сертификат GigaChat через PowerShell-скрипт
- Создан файл `certs/giga_chain.pem`
- Добавлена переменная `GIGACHAT_CA_BUNDLE=certs/giga_chain.pem` в `.env`
- Изменён код `ai/gigachat.py` для использования сертификата

**Файл:** `ai/gigachat.py`, функция `_get_verify_flag()`

---

### Проблема 2: GigaChat OAuth 400 Error

**Ошибка:**
```
OAuth error 400 (пустой ответ)
```

**Причина:** неверный endpoint и отсутствие заголовков

**Решение:**
- Изменён URL на `https://ngw.devices.sberbank.ru:9443/api/v2/oauth`
- Добавлен `Content-Type: application/x-www-form-urlencoded`
- Добавлен `Accept: application/json`
- Добавлен заголовок `RqUID` (UUID для запроса)
- Изменён формат данных на `data=payload` вместо `json=payload`

**Файл:** `ai/gigachat.py`, функция `get_access_token()`

---

### Проблема 3: Telegram Bot 409 Conflict Error

**Ошибка:**
```
Error code: 409. Description: Conflict: terminated by other getUpdates request
```

**Причины:**
1. Несколько экземпляров бота с одним токеном
2. Активный webhook на сервере Telegram

**Решение:**
- Остановлены все процессы `python.exe` через `taskkill`
- Добавлен `bot.remove_webhook()` при запуске бота
- Вручную вызван `deleteWebhook` через Telegram Bot API
- Убедились, что запущен только один экземпляр

**Файл:** `telebot/summary_bot.py`, функция `main()`

---

### Проблема 4: Telethon не видит сообщения от бота

**Причина:** Telethon был авторизован под другим аккаунтом Telegram

**Решение:**
- Изменено `SESSION_NAME` в `.env` (было `tg_session`, стало другое)
- Удалён старый файл сессии `tg_session.session`
- Повторная авторизация с правильного аккаунта
- Добавлен `bot_username=@ED_Zerocoder_intensive` для отслеживания нужного бота

**Файл:** `telethon/config.py`, `telethon/main.py`

---

## 📝 История изменений

### Этап 1: Настройка GigaChat API
- ✅ Создано виртуальное окружение `.venv`
- ✅ Установлены зависимости (`requirements.txt`)
- ✅ Добавлена команда `python ai/main.py token` для получения OAuth токена
- ✅ Решена проблема SSL-сертификатов
- ✅ Исправлен OAuth endpoint

### Этап 2: Простой Telegram-бот
- ✅ Создан `telebot/bot.py` на библиотеке `pyTelegramBotAPI`
- ✅ Интегрирован `ask_gigachat()` для ответов на вопросы
- ✅ Добавлено сохранение сообщений в БД
- ✅ Решена проблема 409 Conflict

### Этап 3: Telethon для сбора сообщений
- ✅ Создан `telethon/main.py` для прослушивания Telegram
- ✅ Реализована работа с БД (`telethon/db.py`)
- ✅ Добавлен фильтр по username бота
- ✅ Решена проблема авторизации Telethon

### Этап 4: Саммаризация по команде (текущая версия)
- ✅ Удалена фоновая саммаризация (старый `summarizer_bot.py`)
- ✅ Создан `telebot/summary_bot.py` с командами:
  - `/status` — показать количество необработанных сообщений
  - `/summary` — создать саммаризацию всех новых
- ✅ Реализована логика объединения текстов и отправки в GigaChat
- ✅ Добавлено автоматическое помечание сообщений как обработанных

---

## 🎯 Текущее состояние (09.12.2024)

### Работающие процессы:
1. ✅ **Telethon** — слушает сообщения, сохраняет в БД
2. ✅ **Summary Bot** — ждёт команды `/summary`

### База данных:
- Всего сообщений: **38**
- Необработанных: **4**

### Статус компонентов:
- ✅ GigaChat API — работает, токен актуален
- ✅ Telegram Bot — работает, webhook удалён
- ✅ Telethon — авторизован, слушает нужный бот
- ✅ База данных — инициализирована, заполняется

---

## 🚀 Что дальше (возможные улучшения)

1. **Фильтрация сообщений** — саммаризировать только определённые чаты
2. **Настройка длины саммари** — краткое/среднее/полное
3. **Планировщик** — автоматическая саммаризация раз в день/неделю
4. **Экспорт результатов** — сохранение саммари в файлы
5. **Web-интерфейс** — просмотр статистики и истории
6. **Аналитика** — графики активности, частота сообщений

---

## 🌐 Публикация через CloudPub.ru (10.12.2024)

### Проблема
Необходимо сделать Flask Dashboard и Telegram Mini App доступными из интернета для использования в Telegram.

### Решение
Интегрирован **CloudPub.ru** — российский аналог ngrok для создания туннелей.

### Реализация

#### 1. Создана документация
- **CLOUDPUB_SETUP.md** — подробная инструкция по настройке CloudPub
- **QUICKSTART_CLOUDPUB.txt** — краткая памятка для быстрого старта
- Обновлен **README.md** с информацией о CloudPub

#### 2. Созданы скрипты для автоматизации

**start_tunnels.ps1** (PowerShell):
```powershell
# Регистрация и запуск туннелей
clo register http 5000 --name flask-dashboard
clo register http 5001 --name mini-app
clo run
```

**start_tunnels.py** (Python):
- Проверка установки CloudPub CLI
- Проверка авторизации
- Автоматический запуск туннелей для обоих портов
- Красивый вывод с цветами и статусами

**check_status.py** (Python):
- Комплексная проверка статуса всех компонентов проекта
- Проверка портов (Flask 5000, Mini App 5001)
- Проверка базы данных (количество сообщений, статус)
- Проверка CloudPub (установка, авторизация)
- Проверка файлов конфигурации (.env)
- Итоговая статистика с рекомендациями

#### 3. Структура туннелей

```
Flask Dashboard:    localhost:5000 → https://xxxxx.cloudpub.site
Telegram Mini App:  localhost:5001 → https://yyyyy.cloudpub.site
```

#### 4. Настройка Mini App в Telegram

1. Запустить туннель для порта 5001
2. Получить URL (например, https://xxxxx.cloudpub.site)
3. Открыть @BotFather → /mybots → выбрать бота
4. Bot Settings → Menu Button → Edit Menu Button URL
5. Вставить URL туннеля

### Команды для запуска

**Автоматический:**
```powershell
.\start_tunnels.ps1
```

**Вручную:**
```powershell
clo login
clo register http 5000 --name flask-dashboard
clo register http 5001 --name mini-app
clo run
```

**Проверка статуса:**
```powershell
python check_status.py
```

### Преимущества CloudPub
- ✅ Российский сервис (стабильная работа в РФ)
- ✅ Автоматический HTTPS (требуется для Telegram Mini Apps)
- ✅ Простая установка и настройка
- ✅ Графический интерфейс для Windows
- ✅ CLI для автоматизации
- ✅ Возможность использования собственных доменов

### Важные замечания
- URL туннелей могут меняться при перезапуске (бесплатный план)
- Для постоянного URL нужен платный план или собственный домен
- Туннели автоматически используют HTTPS
- Можно запускать несколько туннелей одновременно

---

## 📚 Полезные команды

### Проверка БД
```powershell
python -c "import sqlite3; conn = sqlite3.connect('messages.db'); cursor = conn.cursor(); cursor.execute('SELECT COUNT(*) FROM messages WHERE summarized = 0'); print('Необработанных:', cursor.fetchone()[0]); conn.close()"
```

### Обновление GigaChat токена
```powershell
python scripts/update_token.py
```

### Остановка всех процессов Python
```powershell
taskkill /F /IM python.exe
```

### Проверка webhook Telegram
```powershell
Invoke-WebRequest -Uri "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

### Удаление webhook
```powershell
Invoke-WebRequest -Uri "https://api.telegram.org/bot<ВАШ_ТОКЕН>/deleteWebhook?drop_pending_updates=true"
```

### CloudPub туннели

**Запуск туннелей:**
```powershell
.\start_tunnels.ps1
```

**Или вручную:**
```powershell
clo login
clo register http 5000
clo register http 5001
clo run
```

**Проверка опций:**
```powershell
clo options
```

**Список зарегистрированных сервисов:**
```powershell
clo ls
```

**Остановка туннелей:**
```powershell
Ctrl+C в терминале с clo run
# Или:
clo stop
```

### Проверка статуса всех компонентов
```powershell
python check_status.py
```

---

## 💡 Советы для работы на домашнем ПК

1. **Виртуальное окружение уже настроено** — просто активируйте `.venv`
2. **Файл `.env` содержит все ключи** — ничего не нужно менять
3. **Сессия Telethon сохранена** — повторная авторизация не потребуется (файл `tg_session.session`)
4. **База данных на USB** — все сообщения сохранятся
5. **Если появится 409 ошибка** — выполните `deleteWebhook` и перезапустите бота
6. **Если GigaChat не отвечает** — обновите токен через `python scripts/update_token.py`
7. **Для публикации в интернет** — установите CloudPub и запустите туннели через `.\start_tunnels.ps1`
8. **Проверка статуса системы** — запустите `python check_status.py`

---

## 🔗 Важные ссылки

- **GigaChat Документация:** https://developers.sber.ru/docs/ru/gigachat/api/overview
- **Telegram Bot API:** https://core.telegram.org/bots/api
- **Telethon Документация:** https://docs.telethon.dev/
- **pyTelegramBotAPI:** https://github.com/eternnoir/pyTelegramBotAPI
- **CloudPub:** https://cloudpub.ru
- **CloudPub Документация:** https://cloudpub.ru/docs

---

## 📞 Контакст для AI-ассистента

Если вы работаете с новым AI-ассистентом на домашнем ПК, покажите ему этот файл и скажите:

> "Это проект саммаризации Telegram-сообщений. Вся история работы в `PROJECT_HISTORY.md`. 
> Telethon собирает сообщения в БД, Summary Bot создаёт саммаризацию по команде `/summary`. 
> Система полностью настроена и работает. Нужно продолжить разработку."

---

**Последнее обновление:** 10.12.2024  
**Автор:** AI-ассистент Cursor (Claude Sonnet 4.5)

